namespace BuzzerBot
{
    public enum BuzzerEvent
    {
        APPROVED,
        REJECTED,
        SCHEDULE_APPROVAL,
        GENERATE_ACCESS_LINK,
        TIMEOUT,
        COMPLETED,
        ERROR,
        NOOP,
    }
}
